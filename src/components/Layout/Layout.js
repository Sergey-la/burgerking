import React from 'react';
import Header from './Header';
import './Layout.scss';

function Layout() {
    return (
        <div className="container">
            <Header />
        </div>
    );
}

export default Layout;
